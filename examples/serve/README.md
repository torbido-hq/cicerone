"""Minimal examples for calling the Cicerone serve API.

Run against a live serve container (see README Serve mode / docs/tutorial.md)::

    export CICERONE_SERVE_URL=http://localhost:8000
    export CICERONE_SERVE_TOKEN=tutorial-token
    python examples/serve/python_client.py
    node examples/serve/fetch.mjs
    examples/serve/curl_examples.sh
"""
